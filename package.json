{
  "name": "anontalk-bot",
  "version": "2.0.0",
  "description": "Enhanced anonymous chat bot for Telegram with VIP features and multi-language support",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only apphosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:fast": "firebase deploy --only apphosting",
    "deploy:github": "powershell -ExecutionPolicy Bypass -File deploy-github.ps1",
    "deploy:env": "echo 'Environment variables set in firebase.json'",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests specified'",
    "serve": "node index.js",
    "performance": "curl -s https://anontalk--anontalk-bot-5f3f1.us-central1.hosted.app/performance | jq .",
    "env:check": "echo 'Environment variables configured in firebase.json'",
    "env:set": "echo 'Environment variables set in firebase.json'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "telegram",
    "bot",
    "anonymous",
    "chat",
    "firebase",
    "vip",
    "multi-language"
  ],
  "author": "AnonTalk Bot Team",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "compression": "^1.8.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase": "^10.7.1",
    "firebase-admin": "^12.0.0",
    "telegraf": "^4.15.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
